import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    alignItems: "center",
    justifyContent: "center",
    paddingHorizontal: 16,
    paddingVertical: 32,
    backgroundColor: "#048b7e30",
    borderRadius: 10,
    minHeight: 100,
    width: "45%",
  },
  cardText: {
    fontWeight: "700",
    textAlign: "center",
  },
});
